// © Copyright 2016 IBM Corp. Licensed Materials – Property of IBM.

package client

const (
	// SECRETS is the path for secrets in barbican
	SECRETS = "/v1/secrets"
	// ORDERS is the path for orders in barbican
	ORDERS = "/v1/orders"
	// PAYLOAD is the path for getting secret payloads in barbican
	PAYLOAD = "/payload"
	//METADATA is the path for getting user defined metadata in barbican
	METADATA = "/metadata"
)
